<h1> Notes </h1>
<hr>
<%= link_to 'New Post-it', new_note_path%>
<% @notes.each do |note|%>
<h2> <%= note.title %> </h2>
<%= link_to 'Show Post-it', note_path(note)%>
<%= link_to 'Edit Post-it', edit_note_path(note.id)%>
<%= link_to 'Delete Post-it', note_path(note.id), method: :delete%>
<% end %>

<%# In the index.html (home) it will have a page titled Notes and will print each Note title and a button link that will say 'show post-it note' and will have a path to each seperate note %>
