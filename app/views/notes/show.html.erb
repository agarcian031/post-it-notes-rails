<h1> <%= @note.title %> </h1>
<h4> <%= @note.author %> </h4>
<p> <%= @note.body %> </p>
<%= link_to 'Back', notes_path %>

<%# will show each note title, author, body, and have a link that goes back to the index.html %>

